streamlit>=1.42
yfinance>=0.2.40
pandas>=2.0
trendspy>=0.1.4
newsapi-python>=0.2.7
requests>=2.31
lxml>=5.0
plotly>=5.20

# --- Added for the admin-only Rational Compounder data refresh panel ---
openpyxl>=3.1

# --- Added for the optional live grammar/wording check on Company
# Potential free text (only runs if ANTHROPIC_API_KEY is set) ---
anthropic>=0.40

# --- Added for the optional subscription paywall (see paywall_engine.py).
# Both are cheap/no-op to install and stay completely inert unless
# PAYWALL_ENABLED=true is set on Railway. ---
Authlib>=1.3.2
stripe>=10.0

# --- Added for the indexable blog (server.py / blog_render.py).
# server.py is now the app's entrypoint: it serves /blog, /sitemap.xml and
# /robots.txt as real HTML that search engines can read, and proxies every
# other URL - including Streamlit's websocket - through to the Streamlit
# app exactly as before. ---
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
websockets>=12.0
markdown>=3.5
